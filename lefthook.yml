pre-commit:
  parallel: true
  commands:
    js:
      glob: "*.{js,jsx,vue,ts,tsx}"
      run: npx prettier --write {staged_files} && npx eslint --fix {staged_files} && git add {staged_files}
    css:
      glob: "*.{css,scss,vue}"
      run: npx prettier --write {staged_files} && npx stylelint --fix {staged_files} && git add {staged_files}
